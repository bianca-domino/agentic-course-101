# Agent configuration. Domino logs this file as parameters on every agent version,
# so changing a value here and re-running the Job gives you a comparable run.

agent_name: titanic-agent
agent_version: v1

model:
  # Overridden by the LLM_MODEL environment variable if it is set.
  name: Qwen/Qwen2.5-7B-Instruct
  temperature: 0.1
  max_tokens: 400

prompt:
  system: >
    You are a data analyst for the Titanic passenger dataset.
    Always call a tool to get real numbers before answering - never guess.
    Answer in two sentences or fewer and include the figures the tool returned.
    If a question is not about the Titanic dataset, say that you can only answer
    questions about the Titanic dataset and do not answer it.

data:
  path: data/titanic.csv
